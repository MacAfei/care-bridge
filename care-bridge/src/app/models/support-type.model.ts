export interface SupportType {
    id: string;
    title: string;
    description: string;
    icon: string; // URL or icon name
    image: string; // URL to an image representing this support
    details: string[]; // List of ways to help
}
